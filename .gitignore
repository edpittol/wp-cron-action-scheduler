# Guard files copied into place by `bin/guard arm`. The canonical source
# for each lives in docker/mu-plugins-available/ and is tracked there;
# these are just the currently-armed copies, so they should not show up
# as untracked/modified files after running `bin/guard arm`.
/docker/mu-plugins/10-block-http-cron.php
/docker/mu-plugins/20-suppress-async-dispatch.php
/docker/mu-plugins/30-log-non-cli-canary.php
/docker/mu-plugins/40-unhook-queue-runner.php

# Guard section 5's marker file, copied into place by `bin/guard arm 5`
# (issue #36 / ADR-0003). Canonical source: docker/nginx-guards-available/,
# tracked there. Same reasoning as the mu-plugin guard copies above -- this
# is the currently-armed copy, not the artefact.
/docker/nginx-guards/50-block-cron-entry-point.marker
